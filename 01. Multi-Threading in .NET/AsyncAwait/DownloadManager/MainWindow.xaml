<Window x:Class="DownloadManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DownloadManager"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="800">
    <DockPanel Background="AliceBlue">
        <Label DockPanel.Dock="Top" Name="lblDownloadingInstr" FontSize="14">
            Enter Page Addresses
        </Label>
        <StatusBar DockPanel.Dock="Bottom" Background="Beige">
            <StatusBarItem>
                <TextBlock Name="statBarText" Text="Ready"/>
            </StatusBarItem>
        </StatusBar>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="150" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--<TextBox x:Name="textBox" AcceptsReturn="True" FontSize="14" BorderBrush="Blue"
           VerticalScrollBarVisibility="Auto" 
           HorizontalScrollBarVisibility="Auto"
           Margin="0,0,0,82"
           Grid.RowSpan="1"/>-->
            <ListBox x:Name="listBox" Grid.Row="0" d:ItemsSource="{d:SampleData ItemCount=0}" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="5,0,5,0" Grid.Row="1">
                <ToggleButton x:Name="popupBtn" Margin="5,0,5,0" Grid.Row="1" Padding="4, 2"  Height="30">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" Margin="5,0,5,0" Grid.Row="0">
                        <!--TextBlock acts as content element for the popup button-->
                        <TextBlock x:Name="txtBlock">
                            Add
                        </TextBlock>
                            <!--Popup will be bound to Add button"-->
                        <Popup x:Name="addPopup" Placement="Center" AllowsTransparency="True" Height="200" Width="550"
        DataContext="{Binding ElementName=popupBtn}"  IsOpen="True">
                                <Grid x:Name="popupGrid" Background="AliceBlue">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                            <!--<Border BorderBrush="Black" Grid.Row="0" BorderThickness="2" Background="AliceBlue">-->
                                <TextBox x:Name="txtBox"  HorizontalAlignment="Center"  FontSize="14" 
                  VerticalAlignment="Center" Height="30" Width="350" TextAlignment="Left"  BorderBrush="Blue" Margin="0,0,0,82"/>
                                <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" Margin="5,0,5,0" Grid.Row="2">
                      
                                    
                                    <Button x:Name="popupCancelBtn" Content="Cancel" Grid.Row="2" HorizontalAlignment="Right" 
                                        VerticalAlignment="Bottom"
                                        Margin="5,0,5,0" Padding="4, 2" Height="30" Click="discardUrls"/>
                                    <Button x:Name="popupOkBtn"  Content="OK"  Grid.Row="2" HorizontalAlignment="Right" IsEnabled="False" 
                                        Margin="5,0,5,0" Padding="4, 2" Height="30" Click="AddUrls"/>
                                </StackPanel>
                                <!--</Border>-->
                        </Grid>
                        </Popup>
                    </StackPanel>
                </ToggleButton>
                <Button x:Name="downloadBtn" Content="Download" Margin="5,0,5,0" Padding="4, 2" Grid.Row="1" Height="30" 
                        Click="DownloadPages"/>
                <Button x:Name="cancelBtn" Content="Cancel" IsEnabled="False" Margin="5,0,5,0" Padding="4, 2" Grid.Row="1" Height="30" 
                        Click="CancelDownloading"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>
